import 'package:flutter/material.dart';
class century_page extends StatefulWidget {
  const century_page({Key? key}) : super(key: key);

  @override
  State<century_page> createState() => _century_pageState();
}

class _century_pageState extends State<century_page> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Heyy"),),
      body: Container(child: Center(child: Text("cjabsciucbiusbwc"),),),
    );
  }
}
